Winograd is vectorized for RISC-V Vector and has been extracted from the NNPACK repository. 

Download:
    git clone https://github.com/soniab/NNPACKRISCV.git

Building:
    use source compilation.sh to make the static compilation for the Winograd kernels from NNPACK. 
    Change the path of the compiler in the compilation.sh file if necessary and run the following command. 
        'source compilation.sh'

